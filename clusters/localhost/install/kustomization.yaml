apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: wac-hospital  # chceme tieto objekty umiestniť do `wac-hospital` namespace
                         # tento zapis preťaží hodnoty "namespace" vo všetkých objektoch
labels:
- pairs:
    app.kubernetes.io/part-of: wac-hospital

resources:
- ../../../apps/gregor-hires-project-ufe
- ../../../apps/gregor-hires-project-webapi

patches:
- path: patches/project-webapi.service.yaml

components:
- ../../../components/version-developers
- https://github.com/FilipHires316/project-webapi//deployments/kustomize/components/mongodb